[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "virtbox"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
virtbox = "virtbox:main"

[tool.setuptools]
py-modules = ["virtbox"]
